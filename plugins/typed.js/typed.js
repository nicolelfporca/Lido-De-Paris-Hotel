var typed = new Typed('#element', {
    strings: ['LOCATION', 'ROOMS', 'PLACES', 'DESTINATION'],
    typeSpeed: 60,
    backSpeed: 60,
    loop: true,
});

var typed = new Typed('#element2', {
    strings: ['LOCATION', 'ROOMS', 'PLACES', 'DESTINATION'],
    typeSpeed: 60,
    backSpeed: 60,
    loop: true,
});

var typed = new Typed('#element3', {
    strings: ['LOCATION', 'ROOMS', 'PLACES', 'DESTINATION'],
    typeSpeed: 60,
    backSpeed: 60,
    loop: true,
});